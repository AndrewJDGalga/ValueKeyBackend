[project]
name = "backend"
version = "0.0.0"
description = "Specialized value key spreadsheet."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.135.2",
    "psycopg[binary,pool]>=3.3.3",
]
